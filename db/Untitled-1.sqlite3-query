-- database: ./todo.db

SELECT * FROM tasks;


CREATE TABLE IF NOT EXISTS test (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    number INTEGER NOT NULL
);

SELECT * FROM test;

INSERT INTO test (name, number) VALUES ('Шахрияр', 597363487);

UPDATE test SET number  = 59736333487 WHERE id = 1;

SELECT * FROM test WHERE id = 2;